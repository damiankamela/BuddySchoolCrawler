parameters:
    base_url: 'https://buddyschool.com'

services:
    AppBundle\Service\WebContent\Request\BuddySchoolSearchRequest:
        arguments:
            - 'GET'
            - '%base_url%/search'